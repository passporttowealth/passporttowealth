#!/bin/bash
# STUB — see engagement/development/finance-clarity-build-spec.md §15 (recovery: generate new passcode, update host + local .env)
echo "not implemented yet — see backlog Epic 2/3/5/6 for sequencing" >&2
exit 1
